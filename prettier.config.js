// prettier.config.js

const config = {
  printWidth: 120,
  proseWrap: 'never',
  endOfLine: 'auto',
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  arrowParens: 'always',
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
